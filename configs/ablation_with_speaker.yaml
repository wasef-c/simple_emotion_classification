  # configs/ablation_study.yaml
  wandb_project: "MSPP_Simple_seed_42"

  # Base template for all experiments
  template_config: &template
    learning_rate: 9e-3
    weight_decay: 5e-6
    use_curriculum_learning: true
    difficulty_method: "euclidean_distance"
    num_epochs: 30
    batch_size: 32
    train_dataset: "MSPP"
    evaluation_mode: "cross_corpus"

    class_weights:
      neutral: 1.0
      happy: 1.0
      sad: 1.0
      anger: 1.0

    curriculum_epochs: 15
    curriculum_pacing: "sqrt"
    use_difficulty_scaling: True
    use_speaker_disentanglement: True

    expected_vad:
        0: [3.0, 2.0, 2.0]  # neutral  (mid valence, slightly higher arousal than 2.0)
        1: [4.0, 3.8, 3.8]  # happy    (high valence, high arousal, high dominance)
        2: [1.8, 2.2, 2.0]  # sad      (low valence, low arousal, low dominance)
        3: [1.8, 4.2, 4.0]  # anger    (low valence, high arousal, high dominance)

  experiments:

    # - <<: *template
    #   name: "Curriculum Only MSPP"
    #   use_curriculum_learning: true
    #   use_difficulty_scaling: false
    #   use_speaker_disentanglement: false
    
    - <<: *template
      name: "Full System + Spkr MSPP"

    # - <<: *template
    #   name: "Spkr Only MSPP"
    #   use_curriculum_learning: false
    #   use_difficulty_scaling: false
    #   use_speaker_disentanglement: true

    # - <<: *template
    #   name: "Scaling Only MSPP"
    #   use_curriculum_learning: false
    #   use_difficulty_scaling: true
    #   use_speaker_disentanglement: false
    
    # - <<: *template
    #   name: "Preset MSPP"
    #   use_curriculum_learning: true
    #   use_difficulty_scaling: false
    #   use_speaker_disentanglement: false
    #   curriculum_type: "preset_order"
    #   # use_curriculum_learning: true

    # - <<: *template
    #   name: "Baseline MSPP"
    #   use_curriculum_learning: false
    #   use_difficulty_scaling: false
    #   use_speaker_disentanglement: false



